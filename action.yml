name: 'QWED Commerce Auditor'
description: 'Verify Universal Commerce Protocol (UCP) transactions — deterministic Decimal + JSON Schema guards.'
author: 'QWED-AI'
branding:
  icon: 'shield'
  color: 'green'

inputs:
  transaction-file:
    description: 'Path to JSON transaction logs'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.transaction-file }}
